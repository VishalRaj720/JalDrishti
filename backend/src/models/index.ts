import { User } from './User';

// Initialize model associations (none needed for 1st month scope)
export const initializeAssociations = (): void => {
    // No associations needed for users-only scope
};

// Export models
export { User };
